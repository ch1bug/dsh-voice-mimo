/**
 * dsh-voice — browser half type declarations.
 *
 * The browser half registers two slot entries (a mic button and a read-aloud
 * button); it exposes no public module surface beyond the loader entry.
 */
export {};
